<h1>View audio file details</h1>

<p>
  <%=h printable_name(@audio_file) %>
  It was created on <%=h @audio_file.created_at %>.
</p>

<p>
Would you like to <%= link_to 'Listen', @audio_file.public_filename,
		:popup => [ 'Audio Player', 'width=300, height=100' ] %> to the file?
</p>

<h2>Description</h2>

<p>
  <%=h printable_description(@audio_file) %>
</p>

<% if @audio_file.writable_by?(current_user) %>
<h2>Editing Options</h2>
<p>
(<%= link_to 'Edit audio file', {:action => :edit, :id => @audio_file}, :method => :post %> | 
<%= link_to 'Delete audio file', {:action => :destroy, :id => @audio_file}, :method => :post, 
:confirm => "Are you sure?" %>)
<% end %>

<p>
<%= link_to "Back to #{@attached_to_object.to_phrase}", :controller => @attached_to_object.table_name, :action => :show, :id => @audio_file.audible_id %>
</p>

<h1>
  Images

  <% if params[:tag_id] -%>
    matching tag "<%= Tag.find(params[:tag_id]) -%>"
  <% end -%>

</h1>

<p>
  There are <%= @images.size -%> images 
  <% if !@imageable_type.nil? -%>
    associated with this <%= underscores_to_spaces(@imageable_type) -%><% end -%>.
</p>

<%= show_image_thumbnails -%>

<% if logged_in? && @associated_object.writable_by?(current_user) -%>
  <p>
    <%= link_to 'Add an image to the current object',
	 new_image_path(:imageable_type => @imageable_type,
			:imageable_id => @imageable_id) -%>
  </p>
<% end -%>

<p>Return to <%= get_associated_object_show_link_from_type_and_id(@imageable_type, @imageable_id) -%>.</p>

<h1>Versions for Content Item "<%= @content_item.title -%>"</h1>

<p>
  Below are the revisions that have been saved for the current content item.
</p>

<ul>
  <% @versions.each do |civ| -%>
    <li>
      <%= "Version #{civ.version} #{get_document_status(@content_item, civ)}, "  +
	    "created #{civ.updated_at}" -%> 
      (<%= get_version_options(@content_item, civ) -%>)
    </li>
  <% end -%>
</ul>

<p>
  <%= link_to 'View current content item version', content_item_path(@content_item) -%>
</p>

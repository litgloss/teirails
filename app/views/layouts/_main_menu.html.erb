<div id="main-navigation">
  <ul id="headernav">

    <%= generate_system_menu_items -%>

    <li><%= make_menu_link(:content_items, :content_items_path, 'Content Items') %></li>
    <li><%= make_menu_link(:annotations, :annotations_path, 'Annotations') %></li>
    <li><%= make_menu_link(:search, :search_path, 'Search') %></li>
    <% if logged_in? %>
      <% if current_user.can_act_as?("editor") -%>
	<li>
	  <%= make_menu_link(:editorial_options, 
			     :editorial_options_path, 
			     'Editorial Options') -%>
	</li>
      <% end -%>

    <% end %>

  </ul>
</div>
